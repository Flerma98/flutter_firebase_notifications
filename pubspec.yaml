name: example_push_notifications
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-149.0.dev <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  firebase_core:
  firebase_messaging:
  flutter_local_notifications:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true